{
  "name": "parallel-subql",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "install-ex-lint": "yarn add -D eslint typescript @typescript-eslint/parser @typescript-eslint/eslint-plugin",
    "install-prettier": "npm install --save-dev eslint-config-prettier",
    "build": "tsc -b",
    "prepack": "rm -rf dist && npm build",
    "test": "jest",
    "codegen": "./node_modules/.bin/subql codegen",
    "start:docker": "docker-compose pull && docker-compose up --remove-orphans",
    "interface-build": "yarn generate:defs && yarn generate:meta && tsc -b",
    "generate:defs": "ts-node --skip-project node_modules/.bin/polkadot-types-from-defs --package parallel/api-interfaces --input ./src/api-interfaces",
    "generate:meta": "ts-node --skip-project node_modules/.bin/polkadot-types-from-chain --package parallel/api-interfaces --endpoint ./src/api-interfaces/parallel.json --output ./src/api-interfaces --strict"
  },
  "homepage": "https://github.com/subquery/subql-starter",
  "repository": "github:subquery/subql-starter",
  "files": [
    "dist",
    "schema.graphql",
    "project.yaml"
  ],
  "author": "Parallel Team",
  "license": "Apache-2.0",
  "devDependencies": {
    "@parallel-finance/types": "^1.1.1",
    "@subql/cli": "^0.10.0",
    "@subql/types": "^0.8.1",
    "@typescript-eslint/eslint-plugin": "^4.29.2",
    "@typescript-eslint/parser": "^4.29.2",
    "eslint": "^7.32.0",
    "eslint-config-prettier": "^8.3.0",
    "ts-node": "^8.6.2",
    "typescript": "^4.3.5"
  },
  "resolutions": {
    "@polkadot/api": "^5.2.1",
    "@polkadot/types": "^5.2.1"
  }
}
